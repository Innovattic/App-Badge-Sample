apply plugin: 'com.android.library'
apply plugin: 'kotlin-android'
apply plugin: 'com.innovattic.badge'

android {
    compileSdk = 30
}

badge {
    iconNames = ["@mipmap/ic_lib_2"]
    buildTypes {
        debug {
            enabled = true
            text = "Something"
        }
    }
}
